[phases.setup]
cmd = "curl -fsSL https://bun.sh/install | bash && mv /root/.bun/bin/bun /usr/local/bin/bun"

[phases.build]
cmd = "bun install --frozen-lockfile && bun --cwd web build"

[start]
cmd = "bun --cwd web start"
